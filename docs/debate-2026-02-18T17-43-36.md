# 코드 분석서: 오케스트레이션에서 각 에이전트 별로 워크트리를 만드는 것이 좋아 아님 통합하는 것이 좋아?

> **프로젝트:** vive-md  
> **경로:** `/Volumes/SAMSUNG/apps/projects/vive-md`  
> **날짜:** 2026-02-18  
> **분석자:** Claude Code, Codex CLI  
> **라운드:** 4

---

## 프로젝트 구조

```
docs/
  multi-agent-orchestration-papers-guide.md
  vibe_coding_orchestration_papers_2025_2026_comprehensive_review.md
templates/
  design-system/
    디자인시스템-가이드.md
  nextjs/
    NextJS-개발가이드.md
  react/
    React-개발가이드.md
  security/
    보안-가이드.md
  spring-boot/
    Spring-Boot-개발가이드.md
  vue/
    Vue3-개발가이드.md
  waterfall/
    01-요구사항분석/
      요구사항명세서-SRS.md
      요구사항추적매트릭스-RTM.md
      유스케이스명세서.md
    02-시스템설계/
      API설계서.md
      데이터베이스설계서.md
      시스템아키텍처설계서-SAD.md
      화면설계서.md
    03-상세설계/
      상세설계서.md
    04-구현/
    05-테스트/
      테스트결과보고서.md
      테스트계획서.md
      테스트케이스.md
    06-배포/
      배포계획서.md
      운영가이드.md
    07-유지보수/
      유지보수계획서.md
    08-검토/
      UX검토서.md
      단계별검토-체크리스트.md
      프로젝트관리-산출물.md
    CLAUDE.md
vibe-coding/
  papers/
    multi-agent-orchestration-papers.md
    README.md
  prompts/
    README.md
    예시-프롬프트-모음.md
  resources/
    README.md
  README.md
CLAUDE.md
DOCUMENTATION_INDEX.md
multi-agent-orchestration-papers.md
README.md
```

---

## 토론 내용

### 1라운드: 코드 분석 & 입장 발표

#### 🧠 Claude Code *(61.9s)*

이제 코드베이스를 철저히 분석했다. 본론으로 들어가겠다.

---

# Round 1: 에이전트별 워크트리 분리가 압도적으로 우세하다

## 1. 아키텍처 해부 — 이 프로젝트가 이미 답을 알고 있다

이 코드베이스의 의존성 그래프는 명확하다:

`multi-agent-orchestration-papers.md:180`에서:
```
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
```
`multi-agent-orchestration-papers.md:181-182`:
```
- 각 에이전트가 독립적으로 자기 파일에서 작업 = 팀 독립성
- 머지 단계에서 최적 결과 통합 = 교차 협업
```

Croto 논문의 핵심 구조가 **분리 워크트리 → 머지**로 귀결된다. 통합 워크트리를 주장하려면 이 논문 결과를 정면 반박해야 한다.

## 2. MAST 실패 분류가 통합 워크트리의 사형선고다

`multi-agent-orchestration-papers.md:358-371`의 3대 실패 카테고리:
```
카테고리 1: 시스템 설계 문제
- 부적절한 작업 분배
- 에이전트 역할 중복/충돌

카테고리 2: 에이전트 간 불일치
- 에이전트 간 소통 실패
- 컨텍스트 손실/왜곡
```

1,600+ 트레이스에서 κ=0.88로 검증된 결과다. 통합 워크트리에서 에이전트 A가 `src/api.ts`를 수정하는 동안 에이전트 B가 같은 파일을 건드리면? 역할 중복/충돌이 **구조적으로 불가피**하다. 분리 워크트리는 `[OWN FILES]` + `[DO NOT TOUCH]` (`multi-agent-orchestration-papers.md:379-380`)로 이 문제를 **물리적으로 차단**한다:
```
| 작업 분배 문제 | 파일 소유권 명확 할당 ([OWN FILES] + [DO NOT TOUCH]) |
| 역할 중복 | 각 에이전트에 고유한 [GOAL]과 [FILES] |
```

## 3. 라이프사이클 관리 — TEA 프로토콜이 분리를 요구한다

`multi-agent-orchestration-papers.md:208`:
```
- 에이전트/도구/환경의 **라이프사이클 관리** 개념 → 우리의 워크트리 라이프사이클(preflight → running → merging → cleanup)과 일치
```

`preflight → running → merging → cleanup` — 이 4단계 라이프사이클은 통합 워크트리에서 **구현 불가능**하다. 통합 환경에서 에이전트 A가 running이고 B가 merging이면? cleanup 시점을 어떻게 결정하나? 분리 워크트리에서만 각 에이전트의 라이프사이클을 독립적으로 관리할 수 있다.

## 4. 가변 상태 공유 — 이 코드베이스가 경고하는 치명적 함정

`vibe-coding/README.md:229`:
```
- 동시 에이전트 간 가변 상태 공유 → 데이터 불일치
```

이것이 바로 통합 워크트리의 본질이다. 파일 시스템 자체가 가변 상태다. 에이전트 A가 `package.json`에 의존성을 추가하고, B도 동시에 추가하면? **lock file 충돌**이 런타임에 발생한다. 분리 워크트리에서는 이것이 **머지 타임 충돌**로 바뀌어 제어 가능해진다.

## 5. Maker-Checker 패턴의 물리적 전제조건

`multi-agent-orchestration-papers.md:516-518`:
```
Developer Agent → 코드 작성/커밋
QA Agent → 워크트리에서 테스트 실행, 코드 리뷰
```

QA Agent가 Developer Agent의 **커밋된 결과물**을 리뷰한다. 통합 워크트리에서는 Developer가 아직 작성 중인 불완전한 상태를 QA가 보게 된다. 분리 워크트리 + 커밋 기반 핸드오프만이 **원자적 검증**을 보장한다.

## 판결

**에이전트별 워크트리 분리가 유일한 정답이다.** Croto 논문(`multi-agent-orchestration-papers.md:176`)이 품질 향상을 입증했고, MAST(`multi-agent-orchestration-papers.md:354`)가 통합 모델의 실패를 분류했으며, TEA 프로토콜(`multi-agent-orchestration-papers.md:208`)이 독립 라이프사이클을 요구한다. 통합 워크트리를 옹호하는 건 1,600개 실패 트레이스를 무시하는 것이다.

#### ⚡ Codex CLI *(161.1s)*

1) 아키텍처 부검부터 말하면, 이 레포는 실행 오케스트레이터가 아니라 문서 허브라서 “실행 import 그래프”가 아니라 “문서 참조 그래프”가 실체다 (`CLAUDE.md:8`, `CLAUDE.md:14`).  
실제 의존성은 `README.md → vibe-coding/papers/README.md → vibe-coding/papers/multi-agent-orchestration-papers.md`와 `docs/...guide.md → ../multi-agent-orchestration-papers.md`로 갈라져 소스 오브 트루스가 분기된다 (`README.md:13`, `vibe-coding/papers/README.md:9`, `docs/multi-agent-orchestration-papers-guide.md:11`).  
```md
# CLAUDE.md:8-15
templates/
├── spring-boot/Spring-Boot-개발가이드.md
...
├── security/보안-가이드.md
└── waterfall/

# README.md:13-14
| 가이드 | [`vibe-coding/README.md`](./vibe-coding/README.md) |
| 논문 | [`vibe-coding/papers/`](./vibe-coding/papers/) |

# vibe-coding/papers/README.md:9
| [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) |

# docs/multi-agent-orchestration-papers-guide.md:11
**원본 문서**: `../multi-agent-orchestration-papers.md`
```

2) 함수 품질 채점은 `InfiniteVirtualList` D, `useRealtimeNotifications` C-, `loginLimiter` D-, `handleLoginAttempt` F다 (`templates/react/React-개발가이드.md:2499`, `templates/react/React-개발가이드.md:2557`, `templates/security/보안-가이드.md:556`, `templates/security/보안-가이드.md:584`).  
`InfiniteVirtualList`는 effect dependency에 호출 결과를 직접 꽂아서 재실행을 구조적으로 유발한다 (`templates/react/React-개발가이드.md:2523`).  
`handleLoginAttempt`는 read-modify-write를 원자화하지 않아 동시 로그인 실패에서 카운트 유실이 난다 (`templates/security/보안-가이드.md:585`, `templates/security/보안-가이드.md:593`).  
```tsx
// templates/react/React-개발가이드.md:2517-2524
useEffect(() => {
  const lastItem = virtualizer.getVirtualItems().at(-1);
  if (!lastItem) return;
  if (lastItem.index >= allItems.length - 1 && hasNextPage && !isFetchingNextPage) {
    void fetchNextPage();
  }
}, [virtualizer.getVirtualItems(), allItems.length, hasNextPage, isFetchingNextPage, fetchNextPage]);

// templates/security/보안-가이드.md:584-593
async handleLoginAttempt(email: string, success: boolean): Promise<void> {
  const account = await this.getAccountLockInfo(email);
  ...
  const attempts = account.failedAttempts + 1;
  await this.recordFailedAttempt(email, attempts);
}
```

3) 버그 체인은 첫째 `app.get('/ping') → exec(...) → 셸 실행`으로, 루트 원인은 사용자 입력을 명령 문자열에 결합한 설계다 (`templates/security/보안-가이드.md:486`, `templates/security/보안-가이드.md:487`).  
둘째 `app.post('/auth/login', loginLimiter, ...) → keyGenerator(req.body.email||req.ip)`로, 루트 원인은 공격자 제어 필드를 rate-limit key로 사용한 정책 오류다 (`templates/security/보안-가이드.md:571`, `templates/security/보안-가이드.md:560`).  
셋째 `getAccountLockInfo → recordFailedAttempt → lockAccount` 체인으로, 루트 원인은 트랜잭션/원자 증가 부재다 (`templates/security/보안-가이드.md:585`, `templates/security/보안-가이드.md:593`, `templates/security/보안-가이드.md:596`).  
```ts
// templates/security/보안-가이드.md:486-491
app.get('/ping', (req, res) => {
  exec(`ping -c 4 ${req.query.host}`, (err, stdout) => {
    res.send(stdout);
  });
  // 공격: ?host=google.com;rm%20-rf%20/
});

// templates/security/보안-가이드.md:570-572
app.use(globalLimiter);
app.post('/auth/login', loginLimiter, loginHandler);
```

4) 성능 지뢰는 `getVirtualItems()` 배열 의존성으로 인한 불필요 effect 반복과 페이징 폭주 가능성이다 (`templates/react/React-개발가이드.md:2523`, `templates/react/React-개발가이드.md:2521`).  
클라이언트 컴포넌트에서 서버 의도 모듈을 import하면 500KB가 번들에 전파되는 건 문서가 직접 경고한 landmine다 (`templates/nextjs/NextJS-개발가이드.md:2432`, `templates/nextjs/NextJS-개발가이드.md:2437`).  
```ts
// templates/nextjs/NextJS-개발가이드.md:2432-2438
import { parse } from 'some-huge-library'; // 500KB
...
'use client';
import { processData } from '@/lib/heavy-utils'; // 500KB가 클라이언트 번들에 포함!
```

5) 결론은 강하게 간다: 통합 워크트리 고집은 충돌과 상태오염을 구조적으로 키우고, 에이전트별 워크트리 + 순차 머지가 이 코드베이스의 명시적 정답이다 (`vibe-coding/README.md:229`, `vibe-coding/README.md:317`).  
문서가 이미 “독립 파일/모듈 병렬 + git worktree 활용”과 “완료 순서 순차 머지”를 운영 원칙으로 박아놨는데 통합을 주장하면 자기 문서와 정면충돌이다 (`vibe-coding/README.md:311`, `vibe-coding/README.md:317`, `vibe-coding/papers/multi-agent-orchestration-papers.md:180`).  
```md
# vibe-coding/README.md:311-317
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
...
9. **순차 머지**: 완료 순서대로 머지하여 충돌 최소화

# vibe-coding/README.md:229
- 동시 에이전트 간 가변 상태 공유 → 데이터 불일치
```

### 2라운드: 심층 분석 & 반론

#### 🧠 Claude Code *(55.1s)*

코드를 직접 확인 완료. 이제 공격한다.

---

# Round 2: 상대방 주장 팩트체크 — 동의하되 더 깊이 판다

## 1. 상대방의 "소스 오브 트루스 분기" 지적은 맞다 — 그런데 이것이 바로 분리 워크트리의 근거다

Codex가 `docs/multi-agent-orchestration-papers-guide.md:11`의 `**원본 문서**: ../multi-agent-orchestration-papers.md`를 들어 소스 분기를 지적했다. 맞는 말이다. 그런데 **이 중복 구조 자체가 통합 워크트리의 위험을 증명한다.** 에이전트 A가 `vibe-coding/papers/multi-agent-orchestration-papers.md`를, 에이전트 B가 `docs/multi-agent-orchestration-papers-guide.md`를 동시에 편집하면? 통합 워크트리에서는 상대 경로 참조 `../multi-agent-orchestration-papers.md`가 **편집 중 깨질 수 있다.** 분리 워크트리에서는 각각 독립적으로 완료 후 머지 시점에 참조 정합성을 검증한다.

## 2. 상대방이 놓친 핵심 — `[WARNINGS]` 프롬프트가 통합 워크트리의 **소프트웨어적 한계**를 고백한다

`vibe-coding/papers/multi-agent-orchestration-papers.md:464-468`:
```
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
- DO NOT install new dependencies without explicit instruction
- If you are stuck for more than 3 attempts, STOP and commit what you have
- Write a clear commit message explaining what you did and what remains
```

이 프롬프트의 첫 줄을 보라: `DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts`. **프롬프트로 파일 접근을 제한하는 건 희망 사항이지 강제가 아니다.** LLM은 프롬프트를 무시할 수 있다. 분리 워크트리는 이 제약을 **파일시스템 레벨**에서 물리적으로 강제한다 — 다른 에이전트의 워크트리에 파일이 아예 체크아웃되지 않으면 수정 자체가 불가능하다.

## 3. Concurrent 패턴의 전제조건을 상대방이 무시했다

`vibe-coding/README.md:137-148`의 Concurrent 패턴 정의:
```
         ┌→ Agent 1 → Result 1 ─┐
Input →  ├→ Agent 2 → Result 2 ─┼→ Aggregated Result
         └→ Agent N → Result N ─┘
```
`vibe-coding/README.md:146`:
```
- **피해야 할 때**: 에이전트가 서로의 결과를 기반으로 해야 할 때
```

통합 워크트리는 이 "피해야 할 때" 상황을 **구조적으로 강제**한다. 같은 파일시스템을 공유하니까 에이전트 B가 A의 불완전한 중간 결과를 자동으로 본다. 분리 워크트리만이 `Agent 1 → Result 1`이 **완전히 독립된 결과물**임을 보장한다.

## 4. 에이전트 수 제한 근거 — Group Chat의 3개 제한과 워크트리의 관계

`vibe-coding/README.md:161`:
```
- **핵심**: 효과적 제어를 위해 **3개 이하의 에이전트**로 제한 권장
```
`vibe-coding/README.md:328`:
```
14. **3개 이하 동시 에이전트**: 그룹 채팅 연구 — 제어 효과성을 위해 에이전트 수 제한
```

이 제한은 Group Chat(공유 대화 스레드) 패턴에서 나왔다. 통합 워크트리 = 공유 파일시스템 = **Group Chat의 변종**이다. 에이전트가 3개로 제한되는 이유가 바로 공유 상태 때문이다. 분리 워크트리에서는 각 에이전트가 독립 실행되므로 이 제한이 **완화**된다 — Croto(`vibe-coding/papers/multi-agent-orchestration-papers.md:172`)가 "여러 독립 팀이 동시에 다른 솔루션 경로 탐색"을 허용하는 이유다.

## 5. 상대방이 코드 품질 채점으로 논점을 이탈했다

Codex가 `InfiniteVirtualList`, `handleLoginAttempt` 등의 버그를 지적했는데 — 이것들은 **문서 내 예시 코드**지 오케스트레이션 아키텍처와 무관하다. 이 토론의 주제는 "워크트리 분리 vs 통합"이다. React effect dependency 문제(`templates/react/React-개발가이드.md:2523`)는 어떤 워크트리 전략을 쓰든 동일하게 존재한다. **논점 이탈으로 간주한다.**

## 결론

Codex와 나는 결론에서 일치한다 — 분리 워크트리가 정답이다. 하지만 Codex는 근거의 깊이가 얕다. 핵심은 `[WARNINGS]` 프롬프트(`multi-agent-orchestration-papers.md:464-465`)가 **소프트 제약**이라는 점이고, Concurrent 패턴의 전제조건(`vibe-coding/README.md:146`)이 통합 워크트리에서 **구조적으로 위반**된다는 점이다. 프롬프트가 아닌 파일시스템 격리만이 에이전트 간 간섭을 물리적으로 차단한다.

#### ⚡ Codex CLI *(113.0s)*

네 주장, 코드가 직접 반박한다. (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`)

1. **Croto를 “분리 워크트리 강제”로 읽은 건 오독이다.**  
아래 원문은 규범이 아니라 `**Octo Orchestra 적용 포인트**`와 `**개선 아이디어**`다. (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:183`)  
```md
# vibe-coding/papers/multi-agent-orchestration-papers.md:179-183
**Octo Orchestra 적용 포인트**:
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
- 각 에이전트가 독립적으로 자기 파일에서 작업 = 팀 독립성
- 머지 단계에서 최적 결과 통합 = 교차 협업
- **개선 아이디어**: 에이전트 완료 후 서로의 결과를 리뷰하는 "교차 검증" 단계 추가
```

2. **MAST를 “통합 워크트리 사형선고”로 해석한 것도 과장이다.**  
MAST 원문은 실패 원인을 `작업 분배/역할 중복/오케스트레이션 로직 결함`으로 분류할 뿐 파일시스템 토폴로지를 처방하지 않는다. (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:360`)  
```md
# vibe-coding/papers/multi-agent-orchestration-papers.md:357-361
#### 카테고리 1: 시스템 설계 문제
- 부적절한 작업 분배
- 에이전트 역할 중복/충돌
- 오케스트레이션 로직 결함
```
게다가 같은 문서는 “단순한 버그 수정은 단일 에이전트가 더 효율적”이라고 못 박아 네 “유일한 정답” 주장을 스스로 무너뜨린다. (`vibe-coding/papers/multi-agent-orchestration-papers.md:342`, `vibe-coding/papers/multi-agent-orchestration-papers.md:343`)

3. **TEA를 “통합 불가능”이라고 한 건 근거가 없다.**  
TEA 섹션은 `명시적 라이프사이클 + 버전 관리 인터페이스`를 말하고, 저장소 형태 불가능성을 말하지 않는다. (`vibe-coding/papers/multi-agent-orchestration-papers.md:195`)  
```md
# vibe-coding/papers/multi-agent-orchestration-papers.md:208-210
- 에이전트/도구/환경의 **라이프사이클 관리** 개념 → 우리의 워크트리 라이프사이클(preflight → running → merging → cleanup)과 일치
- **버전 관리**: 에이전트 작업 결과를 git 커밋으로 자동 버전 관리 → 이미 적용 중
- **롤백**: 머지 실패 시 브랜치 보존 → 이미 설계 중
```
여기엔 “통합 워크트리 불가”라는 문구가 단 한 줄도 없다. (`vibe-coding/papers/multi-agent-orchestration-papers.md:208`)

4. **루트코즈 체인: 네가 ‘복잡도 게이트’를 건너뛴 게 핵심 오류다.**  
`요구사항 충족` → `가장 낮은 복잡도 선택` → `패턴 선택` → `독립 모듈만 worktree 병렬` → `순차 머지`가 공식 체인인데 네 주장은 1단계를 삭제했다. (`vibe-coding/README.md:125`, `vibe-coding/README.md:123`, `vibe-coding/README.md:311`, `vibe-coding/README.md:317`)  
```md
# vibe-coding/README.md:121-125
| **직접 모델 호출** | ...
| **단일 에이전트 + 도구** | ...
| **멀티에이전트 오케스트레이션** | ...
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.
```

5. **네가 놓친 치명점: 소스 오브 트루스가 이중화되어 라인 인용 신뢰성이 흔들린다.**  
가이드는 루트를 원본이라 적었는데, 색인은 별도 복사본을 가리켜 정책 드리프트가 구조적으로 발생한다. (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`)  
```md
# docs/multi-agent-orchestration-papers-guide.md:11
**원본 문서**: `../multi-agent-orchestration-papers.md`

# vibe-coding/papers/README.md:9
| [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) |
```
실제로 같은 섹션이 `:300` vs `:299`로 이미 어긋나 인용 정확도를 깨뜨린다. (`multi-agent-orchestration-papers.md:300`, `vibe-coding/papers/multi-agent-orchestration-papers.md:299`)

6. **카운터 제안(코드 타깃 명시).**  
`vibe-coding/README.md:311`의 “독립적 파일/모듈은 병렬 실행 (git worktree 활용)” 앞에 `vibe-coding/README.md:125` 규칙을 재명시해 “조건부 worktree”로 바꾸는 게 네 절대화보다 정확하다. (`vibe-coding/README.md:311`, `vibe-coding/README.md:125`)  
`vibe-coding/papers/README.md:9`를 루트 문서 링크로 바꿔 단일 소스 오브 트루스를 강제하는 게 네 방식보다 재현 가능성이 높다. (`vibe-coding/papers/README.md:9`, `docs/multi-agent-orchestration-papers-guide.md:11`)

### 3라운드: 핵심 쟁점 공방

#### 🧠 Claude Code *(54.4s)*

이제 핵심 쟁점 3개에 대해 LINE-BY-LINE 분석이 끝났다. 반격한다.

---

# Round 3: Codex의 3대 반론을 코드로 분해한다

## 쟁점 1: "Croto를 규범으로 읽은 건 오독" — 틀렸다, 절반만 맞다

Codex는 `multi-agent-orchestration-papers.md:179`의 `**Octo Orchestra 적용 포인트**`를 "규범이 아닌 적용 아이디어"로 축소했다. 원문을 다시 보자:

```md
# multi-agent-orchestration-papers.md:176
- 단일 팀 시스템 대비 **소프트웨어 품질 유의미하게 향상**
```
```md
# multi-agent-orchestration-papers.md:180
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
```

`:176`은 **실험 결과**다 — "유의미하게 향상"은 측정 사실이지 제안이 아니다. `:180`의 "이것이 바로 우리의 패턴!"은 감탄사(!)까지 써가며 **이미 적용 중**임을 선언한다. Codex가 이걸 "개선 아이디어"와 동급으로 취급한 건 `:183`의 `**개선 아이디어**`와 `:180`의 `**적용 포인트**`를 섞어 읽은 것이다. 적용 포인트 = 현재 운영 중인 패턴, 개선 아이디어 = 향후 추가 사항. **구분해서 읽어야 한다.**

단, Codex의 "절대적 강제가 아니다"라는 지적 자체는 인정한다. `:342-343`의 원문:
```md
# multi-agent-orchestration-papers.md:342-343
- 단순한 버그 수정은 단일 에이전트가 더 효율적일 수 있음
- **적절한 복잡도 선택**이 중요
```

이건 맞다. 모든 작업에 분리 워크트리가 필요하진 않다. **하지만 멀티에이전트를 쓰기로 결정한 시점에서**, 분리 워크트리가 정답이라는 내 주장은 유효하다.

## 쟁점 2: "MAST가 파일시스템 토폴로지를 처방하지 않는다" — 맞다, 그런데 대응 전략표가 처방한다

Codex가 `:357-361`만 보고 "분류일 뿐 처방 아님"이라 했다. **같은 섹션 20줄 아래를 안 읽었다:**

```md
# multi-agent-orchestration-papers.md:377-380
| 작업 분배 문제 | 파일 소유권 명확 할당 (`[OWN FILES]` + `[DO NOT TOUCH]`) |
| 역할 중복 | 각 에이전트에 고유한 `[GOAL]`과 `[FILES]` |
| 소통 실패 | 현재: 독립 실행 (소통 불필요). 향후: 비서 모니터링 |
```

`:380`을 보라: `현재: 독립 실행 (소통 불필요)`. **독립 실행 = 분리 워크트리다.** 소통 자체가 불필요한 구조를 채택했다는 건 파일시스템 격리를 전제한다. Codex는 분류 파트만 읽고 대응 전략표를 건너뛴 것이다.

## 쟁점 3: "복잡도 게이트를 건너뛰었다" — 이건 Codex가 맞다, 하지만 논점이 다르다

```md
# vibe-coding/README.md:125
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라.
```

인정한다. 나는 1라운드에서 이 전제를 생략했다. **그러나** 이 토론의 전제 자체가 "오케스트레이션에서" — 즉 멀티에이전트를 이미 선택한 상황이다. `:125`의 게이트는 "멀티에이전트를 쓸 것인가"를 결정하는 단계이고, 우리 논쟁은 **그 다음 단계**다. 멀티에이전트 결정 후의 워크트리 전략에서 `:311`이 답이다:

```md
# vibe-coding/README.md:311
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
```

`:465`의 `[WARNINGS]`가 소프트 제약이라는 내 2라운드 주장도 여전히 유효하다:
```md
# multi-agent-orchestration-papers.md:465
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
```

프롬프트 지시 = 소프트 제약. 워크트리 물리 격리 = 하드 제약. **하드 제약이 소프트 제약보다 신뢰도가 높다.** 이건 코드가 아니라 엔지니어링 원칙이다.

## 최종 정리

| 쟁점 | Codex | 내 판정 |
|------|-------|---------|
| Croto 해석 | "규범 아님" | 반만 맞다. `:176` 실험결과 + `:180` 현행 패턴은 규범이다 |
| MAST 처방 | "분류일 뿐" | 틀렸다. `:380` 대응표가 "독립 실행"을 명시한다 |
| 복잡도 게이트 | "전제 누락" | 맞다. 하지만 토론 스코프 밖이다 |

#### ⚡ Codex CLI *(139.6s)*

1. **Croto 해석 승부**: Claude의 `"분리 워크트리 유일 정답"` 해석보다 Codex의 `"적용 포인트(권장)"` 해석이 원문 섹션 타이틀과 문장 톤에 더 정확합니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:183`).  
```md
**Octo Orchestra 적용 포인트**:
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
- 각 에이전트가 독립적으로 자기 파일에서 작업 = 팀 독립성
- 머지 단계에서 최적 결과 통합 = 교차 협업
- **개선 아이디어**: 에이전트 완료 후 서로의 결과를 리뷰하는 "교차 검증" 단계 추가
```
근본 원인은 “논문 결과”와 “프로젝트 적용 메모”를 같은 강도로 읽은 해석 오류입니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:162`, `vibe-coding/papers/multi-agent-orchestration-papers.md:179`).  
피해 범위는 `git worktree` 정책 문구가 4개 파일로 복제되어 정책 오독이 문서 전역으로 전파된다는 점입니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:180`, `multi-agent-orchestration-papers.md:180`, `docs/multi-agent-orchestration-papers-guide.md:45`, `vibe-coding/README.md:311`).  
수정 blast radius는 정책 문구 정합화 4파일이며 미수정 시 “항상 분리”가 기본값으로 고정되어 단순 작업에도 과도한 오케스트레이션을 유도합니다 (`vibe-coding/README.md:125`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`).

2. **MAST/TEA 절대화 승부**: MAST는 실패 분류 체계이지 파일시스템 토폴로지 처방서가 아니므로 Claude의 “통합 사형선고”는 코드 근거가 약합니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:360`).  
```md
#### 카테고리 1: 시스템 설계 문제
- 부적절한 작업 분배
- 에이전트 역할 중복/충돌
- 오케스트레이션 로직 결함
```
동일 문서가 “단순 버그 수정은 단일 에이전트가 더 효율적”이라고 명시하므로 “유일한 정답” 주장은 자기모순입니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:342`, `vibe-coding/papers/multi-agent-orchestration-papers.md:343`).  
```md
- 단순한 버그 수정은 단일 에이전트가 더 효율적일 수 있음
- **적절한 복잡도 선택**이 중요
```
TEA도 “명시적 라이프사이클 + 버전 관리 인터페이스”를 말할 뿐 “통합 불가능”을 말하지 않습니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:195`, `vibe-coding/papers/multi-agent-orchestration-papers.md:208`).  
피해 범위는 “가장 낮은 복잡도” 규칙이 3파일에 존재하는데 절대화 주장이 이를 덮어버린다는 점입니다 (`vibe-coding/README.md:125`, `vibe-coding/papers/multi-agent-orchestration-papers.md:343`, `multi-agent-orchestration-papers.md:344`).

3. **강제성/실행체인 승부**: `[WARNINGS]`는 프롬프트 규칙이며 런타임 강제 장치라는 코드가 없습니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:464`, `vibe-coding/papers/multi-agent-orchestration-papers.md:465`).  
```md
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
```
완전 체인은 여기서 성립합니다: `entry(app.get)` → `middleware(authenticate)` → `handler(inline)` → `database(User.findById)` → `response(res.json/res.status)` (`templates/security/보안-가이드.md:94`, `templates/security/보안-가이드.md:95`, `templates/security/보안-가이드.md:98`, `templates/security/보안-가이드.md:99`).  
```javascript
app.get('/api/users/:id/profile', authenticate, (req, res) => {
  if (req.user.id !== req.params.id && !req.user.hasRole('admin')) {
    return res.status(403).json({ error: 'Forbidden' });
  }
  const user = await User.findById(req.params.id);
  res.json(user);
});
```
반대로 `/auth/login` 체인은 `entry+middleware`까지만 있고 `loginHandler` 구현/DB/response가 문서 내에 없어 체인이 끊깁니다 (`templates/security/보안-가이드.md:556`, `templates/security/보안-가이드.md:571`).  
피해 범위는 `[WARNINGS]` 표기 3파일, 실제 금지 문구 2파일이라 “강제됨”으로 잘못 읽으면 충돌 방지를 프롬프트에 과신하게 됩니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:464`, `multi-agent-orchestration-papers.md:465`, `docs/multi-agent-orchestration-papers-guide.md:112`, `vibe-coding/papers/multi-agent-orchestration-papers.md:465`, `multi-agent-orchestration-papers.md:466`).

정정: 내 1라운드에서 React/보안 예시 품질 채점은 본 토픽(워크트리 전략) 직접 증명에는 과했고, 이 지적은 타당합니다 (`templates/react/React-개발가이드.md:2523`, `vibe-coding/README.md:117`, `vibe-coding/README.md:127`).

### 4라운드: 합의 — 분석서 & 가이드 작성

#### 🧠 Claude Code *(84.0s)*

모든 핵심 코드를 확인했다. 이제 최종 합의 보고서를 작성한다.

---

# 최종 합의 보고서: 에이전트별 워크트리 분리 vs 통합

## 1. 핵심 발견사항 (Key Findings)

### 발견 1: 복잡도 게이트 조건이 문서 전역에서 일관되게 누락됨
- **문제**: git worktree 분리를 권장하는 모든 문서가 "멀티에이전트를 써야 하는지"의 전제 조건을 동시에 명시하지 않는다
- **근거 코드**:
  ```md
  // vibe-coding/README.md:125
  > 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.

  // vibe-coding/README.md:311
  6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
  ```
- **근본 원인**: `:311`이 `:125`의 게이트를 참조하지 않아, 독자가 "항상 worktree 분리"로 오독할 수 있다
- **영향 범위**: `vibe-coding/README.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md`
- **심각도**: HIGH

### 발견 2: `[WARNINGS]` 프롬프트가 유일한 파일 접근 제어 장치임
- **문제**: 에이전트 간 파일 격리가 프롬프트 지시(소프트 제약)에만 의존한다
- **근거 코드**:
  ```md
  // vibe-coding/papers/multi-agent-orchestration-papers.md:465
  - DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
  ```
- **근본 원인**: LLM은 프롬프트를 위반할 수 있으므로, 파일시스템 격리(하드 제약) 없이는 머지 충돌이 **확률적으로 발생**한다
- **영향 범위**: `vibe-coding/papers/multi-agent-orchestration-papers.md:464-468`
- **심각도**: CRITICAL

### 발견 3: MAST 대응 전략표가 "독립 실행"을 현행 정책으로 명시함
- **문제**: MAST 실패 분류는 토폴로지를 직접 처방하지 않지만, 대응 전략표는 분리를 전제로 작성됨
- **근거 코드**:
  ```md
  // vibe-coding/papers/multi-agent-orchestration-papers.md:378-380
  | 작업 분배 문제 | 파일 소유권 명확 할당 (`[OWN FILES]` + `[DO NOT TOUCH]`) |
  | 역할 중복 | 각 에이전트에 고유한 `[GOAL]`과 `[FILES]` |
  | 소통 실패 | 현재: 독립 실행 (소통 불필요). 향후: 비서 모니터링 |
  ```
- **근본 원인**: `:380`의 `독립 실행 (소통 불필요)`는 에이전트가 서로의 상태를 모르는 구조 — 이는 분리 워크트리에서만 자연스러운 모델이다
- **영향 범위**: `vibe-coding/papers/multi-agent-orchestration-papers.md:374-383`
- **심각도**: MEDIUM (사실 확인, 설계 의도 명확화 필요)

### 발견 4: 소스 오브 트루스가 이중화되어 정책 드리프트가 구조적임
- **문제**: 동일 내용이 2개 경로에 복제되어 있고, 한쪽이 다른 쪽을 "원본"이라고만 참조함
- **근거 코드**:
  ```md
  // docs/multi-agent-orchestration-papers-guide.md:11
  **원본 문서**: `../multi-agent-orchestration-papers.md`

  // vibe-coding/papers/README.md:9 (다른 복사본을 가리킴)
  | [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) |
  ```
- **근본 원인**: 루트의 `multi-agent-orchestration-papers.md`와 `vibe-coding/papers/multi-agent-orchestration-papers.md`가 독립 파일로 존재하여 편집 시 동기화 누락이 불가피
- **영향 범위**: `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md`
- **심각도**: HIGH

### 발견 5: Concurrent 패턴의 "피해야 할 때" 조건이 통합 워크트리를 구조적으로 배제함
- **문제**: 통합 워크트리에서는 에이전트가 서로의 중간 결과를 자동으로 참조하게 되어 Concurrent 패턴의 전제가 깨진다
- **근거 코드**:
  ```md
  // vibe-coding/README.md:146
  - **피해야 할 때**: 에이전트가 서로의 결과를 기반으로 해야 할 때

  // vibe-coding/README.md:229
  - 동시 에이전트 간 가변 상태 공유 → 데이터 불일치
  ```
- **근본 원인**: 통합 워크트리 = 공유 파일시스템 = 가변 상태 공유. `:229`가 경고하는 바로 그 상황이 통합 워크트리의 기본값이다
- **영향 범위**: Concurrent 패턴을 사용하는 모든 멀티에이전트 실행
- **심각도**: CRITICAL

---

## 2. 합의된 권장사항 (Agreed Recommendations)

1. **멀티에이전트 사용 시 에이전트별 워크트리 분리가 기본값이다.** `vibe-coding/README.md:311`의 worktree 활용 + `:317`의 순차 머지가 이 코드베이스의 공식 정책이며, `:229`의 가변 상태 공유 경고와 `:465`의 `[WARNINGS]` 소프트 제약 한계를 감안하면 물리적 격리가 유일하게 신뢰할 수 있는 방법이다.

2. **단, 이것은 "멀티에이전트를 선택한 후"의 결정이다.** `vibe-coding/README.md:125`의 복잡도 게이트를 반드시 먼저 통과해야 하며, `:342-343`이 명시하듯 단순 작업은 단일 에이전트가 더 효율적이다.

3. **소스 오브 트루스를 단일화한다.** `docs/multi-agent-orchestration-papers-guide.md:11`이 참조하는 원본과 `vibe-coding/papers/multi-agent-orchestration-papers.md`를 하나로 통합하여 정책 드리프트를 차단한다.

4. **`:311`에 복잡도 게이트 교차 참조를 추가한다.** worktree 분리 권장 문구에 "멀티에이전트가 필요한 경우에 한해" 전제 조건을 명시한다.

5. **`[WARNINGS]`를 보완하는 하드 제약 메커니즘을 문서화한다.** 프롬프트 지시만으로는 파일 접근 통제가 불완전하므로, sparse checkout 등 파일시스템 레벨 격리 방법을 `:464-468` 이후에 추가한다.

---

## 3. 실행 가이드 (Implementation Guide)

### 변경 1: 복잡도 게이트 교차 참조 추가

```md
// BEFORE — vibe-coding/README.md:311
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)

// AFTER
6. **Concurrent 패턴 활용**: 멀티에이전트가 필요한 경우(§복잡도 단계 선택 참조), 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
```

### 변경 2: `[WARNINGS]`에 하드 제약 보완 지침 추가

```md
// BEFORE — vibe-coding/papers/multi-agent-orchestration-papers.md:464-468
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
- DO NOT install new dependencies without explicit instruction
- If you are stuck for more than 3 attempts, STOP and commit what you have
- Write a clear commit message explaining what you did and what remains

// AFTER
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
- DO NOT install new dependencies without explicit instruction
- If you are stuck for more than 3 attempts, STOP and commit what you have
- Write a clear commit message explaining what you did and what remains

[HARD ISOLATION] (프롬프트 위반 방지용 보조 수단)
- git worktree + sparse-checkout으로 에이전트별 [OWN FILES]만 체크아웃
- 예: git worktree add ../agent-A-tree -b agent-A && cd ../agent-A-tree && git sparse-checkout set src/moduleA/
```

### 변경 3: 소스 오브 트루스 단일화

```md
// BEFORE — docs/multi-agent-orchestration-papers-guide.md:11
**원본 문서**: `../multi-agent-orchestration-papers.md`

// AFTER — 루트 파일을 삭제하고 vibe-coding/papers/ 내 단일 파일로 통합
**원본 문서**: [`vibe-coding/papers/multi-agent-orchestration-papers.md`](../vibe-coding/papers/multi-agent-orchestration-papers.md)
```

---

## 4. 우선순위 액션 아이템

| 우선순위 | 항목 | 대상 파일 | 난이도 | 예상 효과 |
|---------|------|----------|--------|----------|
| P0 | 소스 오브 트루스 단일화 (루트 복사본 제거) | `multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md:11` | 낮음 | 정책 드리프트 구조적 차단 |
| P0 | `[WARNINGS]`에 sparse-checkout 하드 격리 지침 추가 | `vibe-coding/papers/multi-agent-orchestration-papers.md:464-468` | 낮음 | 프롬프트 위반 시에도 파일 격리 보장 |
| P1 | worktree 권장 문구에 복잡도 게이트 교차 참조 | `vibe-coding/README.md:311` | 낮음 | "항상 분리" 오독 방지 |
| P1 | MAST 대응표에 "분리 워크트리 전제" 명시 | `vibe-coding/papers/multi-agent-orchestration-papers.md:374` | 낮음 | 설계 의도 명확화 |
| P2 | Concurrent 패턴에 "통합 워크트리 사용 시 주의" 경고 추가 | `vibe-coding/README.md:145-148` | 낮음 | 가변 상태 공유 위험 인식 강화 |

---

**최종 판정**: 양측 모두 **에이전트별 워크트리 분리**가 멀티에이전트 오케스트레이션의 정답이라는 데 합의했다. 핵심 차이는 Codex가 "조건부(복잡도 게이트 통과 후)"를 강조한 반면, Claude Code는 "멀티에이전트 진입 후에는 절대적"을 주장한 점이다. 최종 결론: **멀티에이전트를 쓰기로 결정한 시점에서, 분리 워크트리는 선택이 아닌 필수다.** 단, 그 전 단계에서 `:125`의 복잡도 게이트를 반드시 거쳐야 한다.

#### ⚡ Codex CLI *(5429.1s)*

## 1. 핵심 발견사항 (Key Findings)

### Finding 1
- **문제**: 복잡도 게이트와 worktree 실행 규칙이 같은 문서에서 조건 없이 병치되어 "항상 분리"로 오독되는 구조입니다 (`vibe-coding/README.md:125`, `vibe-coding/README.md:311`, `vibe-coding/README.md:317`).
- **근거 코드**:
```md
// vibe-coding/README.md:125
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.

// vibe-coding/README.md:311-317
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
...
9. **순차 머지**: 완료 순서대로 머지하여 충돌 최소화
```
- **근본 원인**: `규칙(복잡도 선택) -> 패턴 선택 -> worktree 실행`의 호출 체인이 문서에 명시적으로 연결되지 않아 정책 해석이 분기됩니다 (`vibe-coding/README.md:117`, `vibe-coding/README.md:125`, `vibe-coding/README.md:137`, `vibe-coding/README.md:311`).
- **영향 범위**: `vibe-coding/README.md`, `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: CRITICAL (`vibe-coding/README.md:125`, `vibe-coding/README.md:311`).

### Finding 2
- **문제**: Croto 문단이 "적용 포인트"와 "개선 아이디어"를 같은 블록에 섞어 규범 강도를 혼동시킵니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:183`).
- **근거 코드**:
```md
// vibe-coding/papers/multi-agent-orchestration-papers.md:179-183
**Octo Orchestra 적용 포인트**:
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
...
- **개선 아이디어**: 에이전트 완료 후 서로의 결과를 리뷰하는 "교차 검증" 단계 추가

// vibe-coding/papers/multi-agent-orchestration-papers.md:342-343
- 단순한 버그 수정은 단일 에이전트가 더 효율적일 수 있음
- **적절한 복잡도 선택**이 중요
```
- **근본 원인**: 실험 관찰, 로컬 운영 메모, 예외 조건이 한 섹션에 결합되어 절대 명제로 과해석됩니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:176`, `vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`).
- **영향 범위**: `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md`.
- **심각도**: HIGH (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`).

### Finding 3
- **문제**: MAST 실패 "분류"와 프로젝트 "대응 전략"이 분리 표기되지 않아 파일시스템 토폴로지 처방처럼 보입니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:377`).
- **근거 코드**:
```md
// vibe-coding/papers/multi-agent-orchestration-papers.md:357-361
#### 카테고리 1: 시스템 설계 문제
- 부적절한 작업 분배
- 에이전트 역할 중복/충돌
- 오케스트레이션 로직 결함

// vibe-coding/papers/multi-agent-orchestration-papers.md:377-381
| 작업 분배 문제 | 파일 소유권 명확 할당 (`[OWN FILES]` + `[DO NOT TOUCH]`) |
| 역할 중복 | 각 에이전트에 고유한 `[GOAL]`과 `[FILES]` |
| 소통 실패 | 현재: 독립 실행 (소통 불필요). 향후: 비서 모니터링 |
```
- **근본 원인**: "연구 결과"와 "우리 해석"의 레이어 경계가 문서 스키마에 없습니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:351`, `vibe-coding/papers/multi-agent-orchestration-papers.md:375`).
- **영향 범위**: `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: HIGH (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:377`).

### Finding 4
- **문제**: 충돌 회피 정책이 런타임 강제가 아닌 프롬프트 준수에만 의존합니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:463`, `vibe-coding/papers/multi-agent-orchestration-papers.md:465`).
- **근거 코드**:
```md
// vibe-coding/papers/multi-agent-orchestration-papers.md:463-466
**적용**: 에이전트 프롬프트에 추가
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts

// docs/multi-agent-orchestration-papers-guide.md:112
| 방어적 프롬프트 | MAST | `[WARNINGS]` 섹션으로 실패 방어 |
```
- **근본 원인**: `가이드 표 -> 프롬프트 지시 -> 에이전트 자율 준수` 체인에는 파일 잠금이나 CI 게이트가 없습니다 (`docs/multi-agent-orchestration-papers-guide.md:112`, `vibe-coding/papers/multi-agent-orchestration-papers.md:463`, `vibe-coding/papers/multi-agent-orchestration-papers.md:465`).
- **영향 범위**: `docs/multi-agent-orchestration-papers-guide.md`, `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: HIGH (`vibe-coding/papers/multi-agent-orchestration-papers.md:465`).

### Finding 5
- **문제**: 논문 문서의 원본 경로가 이중화되어 정책 드리프트가 구조적으로 발생합니다 (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`).
- **근거 코드**:
```md
// docs/multi-agent-orchestration-papers-guide.md:11
**원본 문서**: `../multi-agent-orchestration-papers.md`

// vibe-coding/papers/README.md:9
| [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) | 주요 논문 요약 및 정리 |
```
- **근본 원인**: canonical source 선언과 인덱스 링크가 서로 다른 파일을 가리키며 동기화 규칙이 없습니다 (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`).
- **영향 범위**: `docs/multi-agent-orchestration-papers-guide.md`, `vibe-coding/papers/README.md`, `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: CRITICAL (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`).

## 2. 합의된 권장사항 (Agreed Recommendations)

1. `vibe-coding/README.md`에 "복잡도 게이트 통과 시에만 worktree 병렬" 문장을 삽입해 조건부 전략으로 고정하세요 (`vibe-coding/README.md:125`, `vibe-coding/README.md:311`); 오버오케스트레이션이 감소합니다 (`vibe-coding/README.md:125`).
2. `multi-agent-orchestration-papers.md`와 `vibe-coding/papers/multi-agent-orchestration-papers.md`에서 Croto 문구를 "권장 패턴"으로 낮추고 예외를 같은 블록에 병기하세요 (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`); 해석 충돌이 줄어듭니다.
3. MAST 섹션에 `[연구결과]`와 `[우리 운영정책]` 라벨을 분리해 증거 계층을 명시하세요 (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:377`); 반박 가능성과 재현성이 올라갑니다.
4. `[WARNINGS]`를 "필수 pre-merge 체크리스트"로 승격해 프롬프트 의존을 줄이세요 (`vibe-coding/papers/multi-agent-orchestration-papers.md:463`, `docs/multi-agent-orchestration-papers-guide.md:112`); 충돌 예방이 하드 프로세스로 이동합니다.
5. 단일 원본을 `multi-agent-orchestration-papers.md`로 선언하고 나머지는 링크 전용으로 바꾸세요 (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`); 문서 드리프트가 차단됩니다.

## 3. 실행 가이드 (Implementation Guide)

```md
// BEFORE — vibe-coding/README.md:125,311
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)

// AFTER — vibe-coding/README.md
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.
> 게이트: 멀티에이전트를 선택한 경우에만 아래 Concurrent/worktree 규칙을 적용한다.
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈에 한해 병렬 실행 (git worktree 활용)
```

```md
// BEFORE — vibe-coding/papers/multi-agent-orchestration-papers.md:179-183
**Octo Orchestra 적용 포인트**:
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
- 각 에이전트가 독립적으로 자기 파일에서 작업 = 팀 독립성
- 머지 단계에서 최적 결과 통합 = 교차 협업
- **개선 아이디어**: ...

// AFTER — vibe-coding/papers/multi-agent-orchestration-papers.md
**Octo Orchestra 적용 포인트(권장)**:
- 멀티에이전트 + 독립 파일 작업일 때 **git worktree 기반 병렬 에이전트**를 사용
- 작업 간 의존성이 높으면 Sequential/Handoff를 우선 선택
- 머지 단계에서 결과 통합
- **개선 아이디어**: ...
```

```md
// BEFORE — docs/multi-agent-orchestration-papers-guide.md:11, vibe-coding/papers/README.md:9
**원본 문서**: `../multi-agent-orchestration-papers.md`
| [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) | 주요 논문 요약 및 정리 |

// AFTER — docs/... , vibe-coding/papers/README.md
**Canonical Source**: `/multi-agent-orchestration-papers.md`
이 문서는 Canonical Source를 참조하는 요약본이다.
| [../../multi-agent-orchestration-papers.md](../../multi-agent-orchestration-papers.md) | Canonical 원본 |
```

## 4. 우선순위 액션 아이템

| 우선순위 | 항목 | 대상 파일 | 난이도 | 예상 효과 |
|---------|------|----------|--------|----------|
| P0 | 단일 원본 선언 및 링크 일원화 | `docs/multi-agent-orchestration-papers-guide.md`, `vibe-coding/papers/README.md` | 낮음 | 정책 드리프트 차단 |
| P1 | 복잡도 게이트 -> worktree 적용 조건 명문화 | `vibe-coding/README.md` | 낮음 | 통합/분리 선택 기준 고정 |
| P1 | Croto 문구를 조건부 권장으로 정정 | `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md` | 중간 | "유일 정답" 오독 제거 |
| P2 | MAST 결과와 운영전략 라벨 분리 | `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md` | 중간 | 증거 계층 명확화 |
| P2 | WARNINGS를 pre-merge 체크리스트로 승격 | `docs/multi-agent-orchestration-papers-guide.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md` | 중간 | 프롬프트 의존 리스크 감소 |

---

## 최종 합의 분석

### 🧠 Claude Code의 분석 종합

모든 핵심 코드를 확인했다. 이제 최종 합의 보고서를 작성한다.

---

# 최종 합의 보고서: 에이전트별 워크트리 분리 vs 통합

## 1. 핵심 발견사항 (Key Findings)

### 발견 1: 복잡도 게이트 조건이 문서 전역에서 일관되게 누락됨
- **문제**: git worktree 분리를 권장하는 모든 문서가 "멀티에이전트를 써야 하는지"의 전제 조건을 동시에 명시하지 않는다
- **근거 코드**:
  ```md
  // vibe-coding/README.md:125
  > 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.

  // vibe-coding/README.md:311
  6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
  ```
- **근본 원인**: `:311`이 `:125`의 게이트를 참조하지 않아, 독자가 "항상 worktree 분리"로 오독할 수 있다
- **영향 범위**: `vibe-coding/README.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md`
- **심각도**: HIGH

### 발견 2: `[WARNINGS]` 프롬프트가 유일한 파일 접근 제어 장치임
- **문제**: 에이전트 간 파일 격리가 프롬프트 지시(소프트 제약)에만 의존한다
- **근거 코드**:
  ```md
  // vibe-coding/papers/multi-agent-orchestration-papers.md:465
  - DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
  ```
- **근본 원인**: LLM은 프롬프트를 위반할 수 있으므로, 파일시스템 격리(하드 제약) 없이는 머지 충돌이 **확률적으로 발생**한다
- **영향 범위**: `vibe-coding/papers/multi-agent-orchestration-papers.md:464-468`
- **심각도**: CRITICAL

### 발견 3: MAST 대응 전략표가 "독립 실행"을 현행 정책으로 명시함
- **문제**: MAST 실패 분류는 토폴로지를 직접 처방하지 않지만, 대응 전략표는 분리를 전제로 작성됨
- **근거 코드**:
  ```md
  // vibe-coding/papers/multi-agent-orchestration-papers.md:378-380
  | 작업 분배 문제 | 파일 소유권 명확 할당 (`[OWN FILES]` + `[DO NOT TOUCH]`) |
  | 역할 중복 | 각 에이전트에 고유한 `[GOAL]`과 `[FILES]` |
  | 소통 실패 | 현재: 독립 실행 (소통 불필요). 향후: 비서 모니터링 |
  ```
- **근본 원인**: `:380`의 `독립 실행 (소통 불필요)`는 에이전트가 서로의 상태를 모르는 구조 — 이는 분리 워크트리에서만 자연스러운 모델이다
- **영향 범위**: `vibe-coding/papers/multi-agent-orchestration-papers.md:374-383`
- **심각도**: MEDIUM (사실 확인, 설계 의도 명확화 필요)

### 발견 4: 소스 오브 트루스가 이중화되어 정책 드리프트가 구조적임
- **문제**: 동일 내용이 2개 경로에 복제되어 있고, 한쪽이 다른 쪽을 "원본"이라고만 참조함
- **근거 코드**:
  ```md
  // docs/multi-agent-orchestration-papers-guide.md:11
  **원본 문서**: `../multi-agent-orchestration-papers.md`

  // vibe-coding/papers/README.md:9 (다른 복사본을 가리킴)
  | [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) |
  ```
- **근본 원인**: 루트의 `multi-agent-orchestration-papers.md`와 `vibe-coding/papers/multi-agent-orchestration-papers.md`가 독립 파일로 존재하여 편집 시 동기화 누락이 불가피
- **영향 범위**: `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md`
- **심각도**: HIGH

### 발견 5: Concurrent 패턴의 "피해야 할 때" 조건이 통합 워크트리를 구조적으로 배제함
- **문제**: 통합 워크트리에서는 에이전트가 서로의 중간 결과를 자동으로 참조하게 되어 Concurrent 패턴의 전제가 깨진다
- **근거 코드**:
  ```md
  // vibe-coding/README.md:146
  - **피해야 할 때**: 에이전트가 서로의 결과를 기반으로 해야 할 때

  // vibe-coding/README.md:229
  - 동시 에이전트 간 가변 상태 공유 → 데이터 불일치
  ```
- **근본 원인**: 통합 워크트리 = 공유 파일시스템 = 가변 상태 공유. `:229`가 경고하는 바로 그 상황이 통합 워크트리의 기본값이다
- **영향 범위**: Concurrent 패턴을 사용하는 모든 멀티에이전트 실행
- **심각도**: CRITICAL

---

## 2. 합의된 권장사항 (Agreed Recommendations)

1. **멀티에이전트 사용 시 에이전트별 워크트리 분리가 기본값이다.** `vibe-coding/README.md:311`의 worktree 활용 + `:317`의 순차 머지가 이 코드베이스의 공식 정책이며, `:229`의 가변 상태 공유 경고와 `:465`의 `[WARNINGS]` 소프트 제약 한계를 감안하면 물리적 격리가 유일하게 신뢰할 수 있는 방법이다.

2. **단, 이것은 "멀티에이전트를 선택한 후"의 결정이다.** `vibe-coding/README.md:125`의 복잡도 게이트를 반드시 먼저 통과해야 하며, `:342-343`이 명시하듯 단순 작업은 단일 에이전트가 더 효율적이다.

3. **소스 오브 트루스를 단일화한다.** `docs/multi-agent-orchestration-papers-guide.md:11`이 참조하는 원본과 `vibe-coding/papers/multi-agent-orchestration-papers.md`를 하나로 통합하여 정책 드리프트를 차단한다.

4. **`:311`에 복잡도 게이트 교차 참조를 추가한다.** worktree 분리 권장 문구에 "멀티에이전트가 필요한 경우에 한해" 전제 조건을 명시한다.

5. **`[WARNINGS]`를 보완하는 하드 제약 메커니즘을 문서화한다.** 프롬프트 지시만으로는 파일 접근 통제가 불완전하므로, sparse checkout 등 파일시스템 레벨 격리 방법을 `:464-468` 이후에 추가한다.

---

## 3. 실행 가이드 (Implementation Guide)

### 변경 1: 복잡도 게이트 교차 참조 추가

```md
// BEFORE — vibe-coding/README.md:311
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)

// AFTER
6. **Concurrent 패턴 활용**: 멀티에이전트가 필요한 경우(§복잡도 단계 선택 참조), 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
```

### 변경 2: `[WARNINGS]`에 하드 제약 보완 지침 추가

```md
// BEFORE — vibe-coding/papers/multi-agent-orchestration-papers.md:464-468
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
- DO NOT install new dependencies without explicit instruction
- If you are stuck for more than 3 attempts, STOP and commit what you have
- Write a clear commit message explaining what you did and what remains

// AFTER
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts
- DO NOT install new dependencies without explicit instruction
- If you are stuck for more than 3 attempts, STOP and commit what you have
- Write a clear commit message explaining what you did and what remains

[HARD ISOLATION] (프롬프트 위반 방지용 보조 수단)
- git worktree + sparse-checkout으로 에이전트별 [OWN FILES]만 체크아웃
- 예: git worktree add ../agent-A-tree -b agent-A && cd ../agent-A-tree && git sparse-checkout set src/moduleA/
```

### 변경 3: 소스 오브 트루스 단일화

```md
// BEFORE — docs/multi-agent-orchestration-papers-guide.md:11
**원본 문서**: `../multi-agent-orchestration-papers.md`

// AFTER — 루트 파일을 삭제하고 vibe-coding/papers/ 내 단일 파일로 통합
**원본 문서**: [`vibe-coding/papers/multi-agent-orchestration-papers.md`](../vibe-coding/papers/multi-agent-orchestration-papers.md)
```

---

## 4. 우선순위 액션 아이템

| 우선순위 | 항목 | 대상 파일 | 난이도 | 예상 효과 |
|---------|------|----------|--------|----------|
| P0 | 소스 오브 트루스 단일화 (루트 복사본 제거) | `multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md:11` | 낮음 | 정책 드리프트 구조적 차단 |
| P0 | `[WARNINGS]`에 sparse-checkout 하드 격리 지침 추가 | `vibe-coding/papers/multi-agent-orchestration-papers.md:464-468` | 낮음 | 프롬프트 위반 시에도 파일 격리 보장 |
| P1 | worktree 권장 문구에 복잡도 게이트 교차 참조 | `vibe-coding/README.md:311` | 낮음 | "항상 분리" 오독 방지 |
| P1 | MAST 대응표에 "분리 워크트리 전제" 명시 | `vibe-coding/papers/multi-agent-orchestration-papers.md:374` | 낮음 | 설계 의도 명확화 |
| P2 | Concurrent 패턴에 "통합 워크트리 사용 시 주의" 경고 추가 | `vibe-coding/README.md:145-148` | 낮음 | 가변 상태 공유 위험 인식 강화 |

---

**최종 판정**: 양측 모두 **에이전트별 워크트리 분리**가 멀티에이전트 오케스트레이션의 정답이라는 데 합의했다. 핵심 차이는 Codex가 "조건부(복잡도 게이트 통과 후)"를 강조한 반면, Claude Code는 "멀티에이전트 진입 후에는 절대적"을 주장한 점이다. 최종 결론: **멀티에이전트를 쓰기로 결정한 시점에서, 분리 워크트리는 선택이 아닌 필수다.** 단, 그 전 단계에서 `:125`의 복잡도 게이트를 반드시 거쳐야 한다.

### ⚡ Codex CLI의 분석 종합

## 1. 핵심 발견사항 (Key Findings)

### Finding 1
- **문제**: 복잡도 게이트와 worktree 실행 규칙이 같은 문서에서 조건 없이 병치되어 "항상 분리"로 오독되는 구조입니다 (`vibe-coding/README.md:125`, `vibe-coding/README.md:311`, `vibe-coding/README.md:317`).
- **근거 코드**:
```md
// vibe-coding/README.md:125
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.

// vibe-coding/README.md:311-317
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)
...
9. **순차 머지**: 완료 순서대로 머지하여 충돌 최소화
```
- **근본 원인**: `규칙(복잡도 선택) -> 패턴 선택 -> worktree 실행`의 호출 체인이 문서에 명시적으로 연결되지 않아 정책 해석이 분기됩니다 (`vibe-coding/README.md:117`, `vibe-coding/README.md:125`, `vibe-coding/README.md:137`, `vibe-coding/README.md:311`).
- **영향 범위**: `vibe-coding/README.md`, `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: CRITICAL (`vibe-coding/README.md:125`, `vibe-coding/README.md:311`).

### Finding 2
- **문제**: Croto 문단이 "적용 포인트"와 "개선 아이디어"를 같은 블록에 섞어 규범 강도를 혼동시킵니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:183`).
- **근거 코드**:
```md
// vibe-coding/papers/multi-agent-orchestration-papers.md:179-183
**Octo Orchestra 적용 포인트**:
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
...
- **개선 아이디어**: 에이전트 완료 후 서로의 결과를 리뷰하는 "교차 검증" 단계 추가

// vibe-coding/papers/multi-agent-orchestration-papers.md:342-343
- 단순한 버그 수정은 단일 에이전트가 더 효율적일 수 있음
- **적절한 복잡도 선택**이 중요
```
- **근본 원인**: 실험 관찰, 로컬 운영 메모, 예외 조건이 한 섹션에 결합되어 절대 명제로 과해석됩니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:176`, `vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`).
- **영향 범위**: `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`, `docs/multi-agent-orchestration-papers-guide.md`.
- **심각도**: HIGH (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`).

### Finding 3
- **문제**: MAST 실패 "분류"와 프로젝트 "대응 전략"이 분리 표기되지 않아 파일시스템 토폴로지 처방처럼 보입니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:377`).
- **근거 코드**:
```md
// vibe-coding/papers/multi-agent-orchestration-papers.md:357-361
#### 카테고리 1: 시스템 설계 문제
- 부적절한 작업 분배
- 에이전트 역할 중복/충돌
- 오케스트레이션 로직 결함

// vibe-coding/papers/multi-agent-orchestration-papers.md:377-381
| 작업 분배 문제 | 파일 소유권 명확 할당 (`[OWN FILES]` + `[DO NOT TOUCH]`) |
| 역할 중복 | 각 에이전트에 고유한 `[GOAL]`과 `[FILES]` |
| 소통 실패 | 현재: 독립 실행 (소통 불필요). 향후: 비서 모니터링 |
```
- **근본 원인**: "연구 결과"와 "우리 해석"의 레이어 경계가 문서 스키마에 없습니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:351`, `vibe-coding/papers/multi-agent-orchestration-papers.md:375`).
- **영향 범위**: `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: HIGH (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:377`).

### Finding 4
- **문제**: 충돌 회피 정책이 런타임 강제가 아닌 프롬프트 준수에만 의존합니다 (`vibe-coding/papers/multi-agent-orchestration-papers.md:463`, `vibe-coding/papers/multi-agent-orchestration-papers.md:465`).
- **근거 코드**:
```md
// vibe-coding/papers/multi-agent-orchestration-papers.md:463-466
**적용**: 에이전트 프롬프트에 추가
[WARNINGS]
- DO NOT modify files not listed in [OWN FILES] — this causes merge conflicts

// docs/multi-agent-orchestration-papers-guide.md:112
| 방어적 프롬프트 | MAST | `[WARNINGS]` 섹션으로 실패 방어 |
```
- **근본 원인**: `가이드 표 -> 프롬프트 지시 -> 에이전트 자율 준수` 체인에는 파일 잠금이나 CI 게이트가 없습니다 (`docs/multi-agent-orchestration-papers-guide.md:112`, `vibe-coding/papers/multi-agent-orchestration-papers.md:463`, `vibe-coding/papers/multi-agent-orchestration-papers.md:465`).
- **영향 범위**: `docs/multi-agent-orchestration-papers-guide.md`, `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: HIGH (`vibe-coding/papers/multi-agent-orchestration-papers.md:465`).

### Finding 5
- **문제**: 논문 문서의 원본 경로가 이중화되어 정책 드리프트가 구조적으로 발생합니다 (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`).
- **근거 코드**:
```md
// docs/multi-agent-orchestration-papers-guide.md:11
**원본 문서**: `../multi-agent-orchestration-papers.md`

// vibe-coding/papers/README.md:9
| [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) | 주요 논문 요약 및 정리 |
```
- **근본 원인**: canonical source 선언과 인덱스 링크가 서로 다른 파일을 가리키며 동기화 규칙이 없습니다 (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`).
- **영향 범위**: `docs/multi-agent-orchestration-papers-guide.md`, `vibe-coding/papers/README.md`, `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md`.
- **심각도**: CRITICAL (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`).

## 2. 합의된 권장사항 (Agreed Recommendations)

1. `vibe-coding/README.md`에 "복잡도 게이트 통과 시에만 worktree 병렬" 문장을 삽입해 조건부 전략으로 고정하세요 (`vibe-coding/README.md:125`, `vibe-coding/README.md:311`); 오버오케스트레이션이 감소합니다 (`vibe-coding/README.md:125`).
2. `multi-agent-orchestration-papers.md`와 `vibe-coding/papers/multi-agent-orchestration-papers.md`에서 Croto 문구를 "권장 패턴"으로 낮추고 예외를 같은 블록에 병기하세요 (`vibe-coding/papers/multi-agent-orchestration-papers.md:179`, `vibe-coding/papers/multi-agent-orchestration-papers.md:342`); 해석 충돌이 줄어듭니다.
3. MAST 섹션에 `[연구결과]`와 `[우리 운영정책]` 라벨을 분리해 증거 계층을 명시하세요 (`vibe-coding/papers/multi-agent-orchestration-papers.md:357`, `vibe-coding/papers/multi-agent-orchestration-papers.md:377`); 반박 가능성과 재현성이 올라갑니다.
4. `[WARNINGS]`를 "필수 pre-merge 체크리스트"로 승격해 프롬프트 의존을 줄이세요 (`vibe-coding/papers/multi-agent-orchestration-papers.md:463`, `docs/multi-agent-orchestration-papers-guide.md:112`); 충돌 예방이 하드 프로세스로 이동합니다.
5. 단일 원본을 `multi-agent-orchestration-papers.md`로 선언하고 나머지는 링크 전용으로 바꾸세요 (`docs/multi-agent-orchestration-papers-guide.md:11`, `vibe-coding/papers/README.md:9`); 문서 드리프트가 차단됩니다.

## 3. 실행 가이드 (Implementation Guide)

```md
// BEFORE — vibe-coding/README.md:125,311
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈은 병렬 실행 (git worktree 활용)

// AFTER — vibe-coding/README.md
> 규칙: **가장 낮은 복잡도**로 요구사항을 충족하라. 불필요한 멀티에이전트는 오버헤드.
> 게이트: 멀티에이전트를 선택한 경우에만 아래 Concurrent/worktree 규칙을 적용한다.
6. **Concurrent 패턴 활용**: 독립적인 파일/모듈에 한해 병렬 실행 (git worktree 활용)
```

```md
// BEFORE — vibe-coding/papers/multi-agent-orchestration-papers.md:179-183
**Octo Orchestra 적용 포인트**:
- 이것이 바로 우리의 **git worktree 기반 병렬 에이전트** 패턴!
- 각 에이전트가 독립적으로 자기 파일에서 작업 = 팀 독립성
- 머지 단계에서 최적 결과 통합 = 교차 협업
- **개선 아이디어**: ...

// AFTER — vibe-coding/papers/multi-agent-orchestration-papers.md
**Octo Orchestra 적용 포인트(권장)**:
- 멀티에이전트 + 독립 파일 작업일 때 **git worktree 기반 병렬 에이전트**를 사용
- 작업 간 의존성이 높으면 Sequential/Handoff를 우선 선택
- 머지 단계에서 결과 통합
- **개선 아이디어**: ...
```

```md
// BEFORE — docs/multi-agent-orchestration-papers-guide.md:11, vibe-coding/papers/README.md:9
**원본 문서**: `../multi-agent-orchestration-papers.md`
| [multi-agent-orchestration-papers.md](./multi-agent-orchestration-papers.md) | 주요 논문 요약 및 정리 |

// AFTER — docs/... , vibe-coding/papers/README.md
**Canonical Source**: `/multi-agent-orchestration-papers.md`
이 문서는 Canonical Source를 참조하는 요약본이다.
| [../../multi-agent-orchestration-papers.md](../../multi-agent-orchestration-papers.md) | Canonical 원본 |
```

## 4. 우선순위 액션 아이템

| 우선순위 | 항목 | 대상 파일 | 난이도 | 예상 효과 |
|---------|------|----------|--------|----------|
| P0 | 단일 원본 선언 및 링크 일원화 | `docs/multi-agent-orchestration-papers-guide.md`, `vibe-coding/papers/README.md` | 낮음 | 정책 드리프트 차단 |
| P1 | 복잡도 게이트 -> worktree 적용 조건 명문화 | `vibe-coding/README.md` | 낮음 | 통합/분리 선택 기준 고정 |
| P1 | Croto 문구를 조건부 권장으로 정정 | `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md` | 중간 | "유일 정답" 오독 제거 |
| P2 | MAST 결과와 운영전략 라벨 분리 | `multi-agent-orchestration-papers.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md` | 중간 | 증거 계층 명확화 |
| P2 | WARNINGS를 pre-merge 체크리스트로 승격 | `docs/multi-agent-orchestration-papers-guide.md`, `vibe-coding/papers/multi-agent-orchestration-papers.md` | 중간 | 프롬프트 의존 리스크 감소 |

